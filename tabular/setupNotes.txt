# Current Notes for setting up autogluon with tabular prediction (on Mac):

# Create virtualenv: python3 -m venv TabularAutoGluon

cd ~/Documents/AutoGluon/githubAutogluon/auto-ml-with-gluon/
source ~/virtual/TabularAutoGluon/bin/activate

pip install --upgrade pip
pip install dask --upgrade
pip install mxnet # mxnet-cu100 for GPU
pip install scikit-optimize mxboard

brew install libomp  # Optional


python setup.py develop # install autogluon
cd tabular/
python setup.py install # install tabular module

